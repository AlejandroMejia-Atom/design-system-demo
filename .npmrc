# StackBlitz mode — private deps come from vendor/*.tgz (no registry auth).
# Public @fortawesome/* must use registry.npmjs.org (lockfile must not point at npm.fontawesome.com).
engine-strict=true
@fortawesome:registry=https://registry.npmjs.org/
